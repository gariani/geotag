import 'package:flutter/material.dart';

/// Icon design system: Brand Orange and shared accent for buttons/icons.
class AppColors {
  AppColors._();

  /// Brand Orange #FF6B00 – primary actions, icons, accents.
  static const Color brandOrange = Color(0xFFFF6B00);
}
